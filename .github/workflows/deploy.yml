name: deploy

# Auto-deploy to Fly.io on every green push to `main`. Gated on `ci`
# (needs both backend + frontend jobs green from the same commit).
on:
  push:
    branches: [main]

concurrency:
  group: fly-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to Fly
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - name: Deploy
        run: >
          flyctl deploy --remote-only
          --build-arg VITE_AB_API_BASE=https://ziffsplit-api.fly.dev
          --build-arg VITE_AB_SITE_KEY=${{ secrets.VITE_AB_SITE_KEY }}
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
